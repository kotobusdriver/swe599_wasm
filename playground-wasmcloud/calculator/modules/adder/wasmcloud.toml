name = "adder-component"
language = "python" 
type = "component"
version = "0.1.0"

[component]
# NOTE: The that the 'build' directory must be available, otherwise the build command will fail.
build_command = "componentize-py -d ./wit -w adder-component componentize app -o build/adder-component.wasm"
build_artifact = "build/adder-component.wasm"
destination = "build/adder-component_s.wasm"
